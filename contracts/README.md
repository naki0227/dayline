# Contracts

This directory owns versioned, language-neutral schemas crossing the Rust/Swift
boundary. The first contract set will define ContextEvent, SemanticArtifact,
ContextBundle, and ActionProposal. Generated code must be reproducible and CI must
reject stale generated output.

