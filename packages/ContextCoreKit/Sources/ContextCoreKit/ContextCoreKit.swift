/// Swift-facing facade metadata for the platform-neutral context engine.
public enum ContextCoreKit {
  /// Must match the schema version exported by `context-domain`.
  public static let schemaVersion: UInt32 = 1
}
